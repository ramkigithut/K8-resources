apiVersion : v1
kind: Pod
metadata:
  name: env
spec:
  containers:
    - name: nginx
      image: nginx
      env:
        - name: course
          value: docker
        - name: practice
          value: docker extensive
        - name: length
          value: regular

